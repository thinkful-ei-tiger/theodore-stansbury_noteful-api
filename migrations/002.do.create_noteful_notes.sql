CREATE TABLE noteful_notes (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    "folderId" INTEGER REFERENCES folders(id) ON DELETE CASCADE NULL,
    name TEXT NOT NULL,
    content TEXT,
    modified TIMESTAMPTZ NOT NULL DEFAULT now()
);